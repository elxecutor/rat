# Implementation Plans

Generated by the improve skill on 2026-07-03 against commit `fd5c459`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Batch-receive upload info instead of char-by-char | P1 | S | — | DONE |
| 002  | Drain incoming file data on upload open failure | P1 | S | 001 (¹) | DONE |
| 003  | Replace predictable temp file with mkstemp | P1 | S | — | DONE |
| 004  | Guard Linux-only modules from Windows cross-build | P1 | M | — | DONE |
| 005  | Add pre-shared key authentication to C2 handshake | P2 | L | 006 (²) | DONE |
| 006  | Extract duplicated helper functions into shared util file | P2 | S | — | DONE |

(¹) Plan 002 edits the same function (`handle_upload`) as 001. They can be
executed in either order but not in parallel — merge conflicts will result.
Do 001 first, then 002.

(²) Plan 005's PSK challenge functions use `send_all`/`recv_all`. If plan 006
has been executed, those functions are available from `util.h`. If 006 has
not been executed, inline the loops in `crypto.c` instead (the plan already
describes both approaches). Either way, 005 does not *require* 006.

## Dependency notes

None of these plans depend on each other in a hard way. 001+002 touch the
same function and share merge risk if done in parallel. 005 is easier after
006 (shared `send_all`/`recv_all`) but has a fallback. All other plans are
independent.

## Findings considered and rejected

- **Hardcoded IP (`127.0.0.1`)** already tracked in `TODO.md`. Effort is trivially small but the TODO exists, so not duplicating as a plan. When someone picks up the TODO, they should accept `-H`/`--host` CLI args.
- **Stale README build instructions** — low leverage; a contributor will notice on the first `make` attempt and self-correct.
- **`-static` flag in module builds** — the flag is harmless (just bloats binary size). Fix if a module build is ever slow enough that it matters.
- **Duplicate persistence implementations (`src/persistence.c` vs `modules/persistence.c`)** — the module version is a standalone CLI tool, the src version is linked into the client. Even though they share logic, they have different entry points and different audiences. Worth a future unification, but not high enough leverage vs. the risk of breaking the standalone tool.
- **Keylogger FD leak** — MED confidence without reading the full module. Flagged for investigation if the keylogger module is actively used.
